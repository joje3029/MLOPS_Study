* 가상환경사용, 본인의 requirements 형상관리!
0. api 서버 구현
1. API콜(start, end)하면 DB에서 조회 후 파일로 저장
2. watchdog에서 파일 생성 감지
3. 파일 로드 후 모델 생성
4. watchdog에서 모델 생성 감지
5. 모델 로드
6. 예측
http://127.0.0.1:9999/api-docs # API문서 주소
Date Open High Low Close Adj Close Volume # 칼럼명
--------------------------------------------------------
1. 요청부서에서는 api로 AI로 예측한 결과를 받기를 원한다.
2. 부서별 사전 협의된 내용은 아래와 같다.
  - 요청부서에서는 시작날짜와 종료날짜를 인자로 콜한다.
  - 데이터 과학자는 lstm으로 모델을 생성하고 시각화까지 구현을 완료하고 소스코드를 인계하였다.
  - 모델 성능이 좋지 않으면 모델을 비교/변경하기로함.
3. 당신은 AI 시스템 담당자로 api서버로 요청부서에 서비스를 제공해야 한다. 구현해야할 사항은 아래와 같다.
  가. 외부시스템에서 시작날짜와 종료날짜의 가격정보를 받아온 후 dataframe -> json으로 변환 후 리턴하는 함수를 구현
  나. json -> dataframe으로 변환 후 모델을 생성, 학습, 저장, 예측하는 모듈을 구현
--------------------------------------------------------------------------------------
12/19 내일 할꺼.
Mlops 할꺼.
지금 post맨으로 넣었을때 되야하고

오늘한거가 DB연결이니까 이걸로 했을때도 Db로 연결해서 나와야함.
->test폴더에 있음.



클라이언트인 lsltm.py는 app_starter.py로 보내고 

app_starter.py가  data_from_yf.py로 하고 

data_from_yf가 json으로 주면 그걸로 보여줌.

=> 왜냐면 서로 통신을 json으로 오가니까. 지금처럼 df로 보내면 안됨. 조진거임.

수정할 부분
lstm.py에 from data_from_yf import data_from를 해서 지금은 data_from_yf한테 직접 보내는데 그게아니라 이친구는 app_starter를 import해야함. 

그리고 지금 app_startersms  @data.route('/') 때문에 엔드포인트로만 받음. 이부분 주의 
즉. 보낼때 엔드로 재주껏 보내든 아니면 진짜 아닌거 같기는 한데  @data.route('/')를 주석하거나. 

지금 상황에서 postman으로 파라미터를 start와 end를 주면 결과값은 안나와도 구동은 되야함.

즉 지금 나는 lstm.py 서버에서 학습을 시키고 하는데 그걸 app_starter.py에서 해서 학습을 해서 해야함.
왜 data_from_yf.py에서 학습이 아닌가 => 이름 봐봐라 쟈는 data를  야후파이낸스에서 가꼬오는아

즉. lstm이 서버가 아니셔.

-------------------------------------------------------------------------
쌤의 요구사항 2가지
1. 포스트맨 -> 이거먼저하고 되면 2를 한다.

일단 이거 같은 경우에는 많이 필요 없고 api_test.py와 data_from_yf(DB가 아니라 야후파이낸스에서 직접 받음), lstm.py만 있으면 됨.

2. Db

